services:
  dev-dashboard:

    container_name: dev-dashboard

    build: .

    stdin_open: true
    tty: true

    volumes:
      - ./projects:/projects
      - /var/run/docker.sock:/var/run/docker.sock

    environment:
      - GITHUB_TOKEN=${GITHUB_TOKEN}