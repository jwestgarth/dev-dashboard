services:
  dev-dashboard:

    container_name: dev-dashboard

    build: .

    command: python -m app.main

    stdin_open: true
    tty: true

    volumes:
      - ./projects:/projects
      - /var/run/docker.sock:/var/run/docker.sock